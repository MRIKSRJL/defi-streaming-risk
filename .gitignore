# Synthetic data and trained models (keep dirs via .gitkeep)
data/*.csv
models/*.json

# Terraform local state and secrets
terraform/.terraform/
terraform/.terraform.lock.hcl
terraform/*.tfstate
terraform/*.tfstate.*
terraform/*.tfvars
